server:
  port: 1704

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/akashkapseAk/FaceBookVistorCompleted
          clone-on-start: true
